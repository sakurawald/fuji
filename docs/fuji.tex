% Preamble
\documentclass[10pt,oneside]{book}

% Packages
% use a4 paper
\usepackage[a4paper, total={6in, 8in}]{geometry}

% color extension
\usepackage{xcolor}

% colored box
\usepackage{tcolorbox}

% hyper link colorize
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    urlcolor=red
}

% the 'outputdir' is reletive to fuji.tex, and must point the 'output directory' of latex compiler.
\usepackage[outputdir=@TOKEN_PROJECT_ROOT@/out/]{minted}

% fix the character '_' can't be copied in output .pdf file
\usepackage[T1]{fontenc}

% disable double hyphen ligature in typeset
\usepackage{microtype}
\DisableLigatures[-]{}

% Macros
\input{macros/macro}

% Document
\begin{document}

    \title{
        \textbf{Fuji Manual}\\[0.5em]
        \large {\version}
    }
    \author{sakurawald}
    \date{\today}

    \maketitle
    \tableofcontents

    \mainmatter

    \input{chapters/concept/concept}
    \input{chapters/permission/permission}
    \input{chapters/meta/meta}
    \input{chapters/configuration/configuration}
    \input{chapters/module/module}
    \input{chapters/qna/qna}
    \input{chapters/transformer/transformer}
    \input{chapters/development/development}
    \input{chapters/suggestion/suggestion}

\end{document}