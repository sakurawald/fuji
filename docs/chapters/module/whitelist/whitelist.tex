\LevelOne{whitelist}

\LevelTwo{Purpose}
This module makes the mojang vanilla whitelist system only compares the \tbf{username} and \tbf{ignore UUD}.

\begin{warn}{Only enable this module in offline-mode server}
    If you are hosting a online-mode server, you will never need to enable this module.
\end{warn}